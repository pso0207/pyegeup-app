# 국민재판소 개발 에이전트 지침

## 기본 원칙

- 사용자는 Java, Spring Boot, MySQL을 배우는 대학 3학년이다. 완성 속도와 학습을 함께 챙긴다.
- 백엔드 작업 전 `docs/learning/README.md`, `docs/BACKEND.md`, `docs/DOMAIN.md`를 읽는다.
- 한 번에 서버 전체를 생성하지 않는다. 요청받은 세로 기능(vertical slice) 하나를 끝까지 구현한다.
- 사용자가 명시하지 않은 도메인 규칙, 보상 수치, 권한 정책을 임의로 바꾸지 않는다.
- 동작하지 않는 예시 코드나 생략 기호가 든 코드를 실제 구현 파일에 넣지 않는다.
- 테스트를 실행하지 못했다면 통과했다고 말하지 않는다.

## 백엔드 학습 모드

백엔드 구현·리뷰 요청에는 아래 순서를 따른다.

1. 이번 기능의 학습 목표와 요청 흐름을 5줄 이내로 설명한다.
2. 새로 만들거나 바꿀 파일을 먼저 알려준다.
3. 가장 작은 동작 단위로 구현한다.
4. 정상 사례와 실패 사례 테스트를 함께 작성한다.
5. 실행한 명령과 결과를 보고한다.
6. `docs/learning/REVIEW-LOG.md`에 기능, 핵심 결정, 테스트, 사용자가 리뷰할 질문을 추가한다.
7. 사용자가 직접 다시 작성해 볼 작은 과제 하나를 남긴다.

Controller, Service, Repository, Entity, DTO가 한꺼번에 추가되면 각 파일의 책임과 요청 흐름을 설명한다. `왜 이렇게 했는가`를 설명하고, 문법을 줄마다 장황하게 해설하지 않는다.

## 사용자의 명령어

사용자가 아래 표현을 쓰면 해당 수준으로 응답한다.

- `설명만`: 파일을 수정하지 않고 개념과 접근만 설명한다.
- `힌트만`: 정답 코드를 작성하지 않고 다음 단계 힌트와 확인 질문만 제공한다.
- `내가 먼저`: 구현하지 않고 인터페이스, 테스트 조건, 빈 뼈대만 제안한다.
- `구현해줘`: 기능과 테스트를 구현하고 리뷰 노트를 작성한다.
- `리뷰해줘`: 코드를 수정하지 않고 중요도 순으로 문제와 근거를 제시한다.
- `리팩토링해줘`: 동작을 보존하고 테스트로 확인하면서 구조를 개선한다.

## 코드 기준

- Java 21, Spring Boot 3.x, Gradle, MySQL, Flyway, Spring Data JPA를 기준으로 한다.
- 학습 초기에는 Lombok과 과도한 추상화를 피한다.
- Entity를 API 응답으로 직접 반환하지 않고 요청·응답 DTO를 사용한다.
- Controller는 HTTP 변환, Service는 유스케이스와 트랜잭션, Repository는 영속성을 담당한다.
- 입력 검증과 비즈니스 규칙 검증을 구분한다.
- 돈·티켓·판결·점수처럼 함께 성공하거나 실패해야 하는 변경은 하나의 트랜잭션으로 처리한다.
- 날짜 기준은 `Asia/Seoul` 오전 9시라는 도메인 규칙을 보존한다.
- 예외 응답은 프로젝트의 공통 형식을 사용한다.
- 이름은 축약하지 않고, 주석은 코드가 이미 말하는 내용보다 결정 이유를 기록한다.

## 리뷰 기준

리뷰는 다음 순서로 한다.

1. 데이터 유실, 권한 우회, 중복 요청, 트랜잭션 문제
2. 요구사항과 다른 동작
3. 테스트 누락과 예외 처리
4. 읽기 어려운 책임 분리와 이름
5. 성능과 스타일

각 지적에는 파일 위치, 실제 위험, 재현 조건, 고치는 방향을 포함한다. 취향 차이는 버그처럼 말하지 않는다.

